steps:
    - checkout: self
    - condition: eq('${{variables.unittest}}', 'true')
    - task: DotNetCoreCLI@2
      displayName: Restore
      inputs:
        command: restore
        projects: '$(solution)'
