steps:
    - checkout: self
    - ${{ if eq( '${{variables.unittest}}', 'true') }}
    - task: DotNetCoreCLI@2
      displayName: Restore
      inputs:
        command: restore
        projects: '$(solution)'
