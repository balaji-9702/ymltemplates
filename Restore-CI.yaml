steps:
    - ${{ if eq('${{variables.unittest}}, 'true') }}:
    - checkout: self
    - task: DotNetCoreCLI@2
      displayName: Restore
      inputs:
        command: restore
        projects: '$(solution)'
